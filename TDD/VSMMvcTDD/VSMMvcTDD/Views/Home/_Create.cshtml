@model VSMMvcTDD.Models.ContactViewModel

@using (Html.BeginForm("Create", "Home", null, FormMethod.Post, new { @id = "contactForm" }))
{
    @Html.AntiForgeryToken()
    @Html.EditorForModel()

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" id="Save" class="btn btn-primary" />
        </div>
    </div>
}